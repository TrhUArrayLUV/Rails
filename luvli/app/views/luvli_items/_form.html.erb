<% if @luvli_item.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@luvli_item.errors.count, "error") %> prohibited this luvli from being saved:</h2>
    <ul>
      <% @luvli_item.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
<%= form.label :content %>
<%= form.text_field :content %>
<%= form.submit "save" %>
